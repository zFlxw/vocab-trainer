export class Config {
  // The IP is the IP of the server. If you want to test it locally, you can use localhost. 
  // However, this is just the Ip4 of my local PC, so that I can access it via other systems in my network.
  static baseUrl: string = "http://0.0.0.0:3001";
}
